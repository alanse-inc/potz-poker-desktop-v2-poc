/**
 * テロップモード
 */
export type TelopMode = "basic" | "classic" | "modern" | "broadcast";

/**
 * プレイヤーグループのテロップ配置
 */
export type PlayerGroupSide = "left" | "right";

/**
 * プレイヤーカードのサイズ
 */
export type PlayerCardSize = "small" | "large";
